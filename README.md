# Next js auth testing

currently testing how make loggin page in next js.

needs .env with '
    ACCESS_TOKEN_SECRET=""
    REFRESH_TOKEN_SECRET=""
    POSTGRES_URL=""
'

and a postgresql database

database configuration on admin page

run with npm run dev